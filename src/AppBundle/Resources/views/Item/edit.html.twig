{% extends '::base.html.twig' %}

{% block body %}
    {% include 'AppBundle::menu.html.twig' %}
    <div class="col-md-9">
        <h1>Item edit</h1>

        {{ form(edit_form) }}

        <ul class="record_actions">
            <li>
                <a href="{{ path('item') }}">
                    Back to the list
                </a>
            </li>
            <li>{{ form(delete_form) }}</li>
        </ul>
    </div>
{% endblock %}
